{
  "name": "switchyard-codex-host-example",
  "runtimeBaseUrlEnv": "SWITCHYARD_RUNTIME_BASE_URL",
  "catalogCommand": "pnpm run switchyard:cli -- host-example --target codex --json",
  "request": {
    "provider": "chatgpt",
    "lane": "web",
    "model": "gpt-4o",
    "input": "Summarize the current provider health snapshot."
  },
  "safeClaims": [
    "partial Codex host example",
    "Responses-style runtime bridge config"
  ]
}
