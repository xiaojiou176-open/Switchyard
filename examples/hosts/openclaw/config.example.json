{
  "name": "switchyard-openclaw-host-example",
  "runtimeBaseUrlEnv": "SWITCHYARD_RUNTIME_BASE_URL",
  "catalogCommand": "pnpm run switchyard:cli -- host-example --target openclaw --json",
  "request": {
    "provider": "chatgpt",
    "lane": "web",
    "model": "gpt-4o",
    "input": "Return the current provider readiness summary before delegation."
  },
  "safeClaims": [
    "partial OpenClaw host example",
    "delegation-first config with preflight"
  ]
}
